import 'package:flutter/material.dart';

void main() {
  runApp(const NotesAppss());
}

class NotesAppss extends StatelessWidget {
  const NotesAppss({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
